# Optional canonical production URL. Netlify's built-in URL is used by default.
# Set this to the campaign domain after it is connected to the Netlify project.
# SITE_URL=https://your-campaign-domain.example

# Netlify supplies CONTEXT and Blobs credentials automatically.
# Use `netlify dev` for local enquiry storage; never commit account tokens.
